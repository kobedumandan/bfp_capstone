alembic==1.18.4
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.13.0
click==8.3.3
colorama==0.4.6
fastapi==0.136.1
GeoAlchemy2==0.19.0
greenlet==3.5.0
h11==0.16.0
httpx==0.28.1
idna==3.13
Mako==1.3.11
MarkupSafe==3.0.3
packaging==26.2
psycopg2-binary==2.9.12
pydantic==2.13.3
pydantic_core==2.46.3
python-dotenv==1.2.2
SQLAlchemy==2.0.49
starlette==1.0.0
typing-inspection==0.4.2
typing_extensions==4.15.0
uvicorn==0.46.0

# ── Numerical / Scientific ────────────────────────────────────────────────────
numpy>=1.24.0
scipy>=1.10.0

# ── Graph Processing ──────────────────────────────────────────────────────────
networkx>=3.0
geopandas>=0.14.0
# osmnx>=1.9.0  # uncomment only if using OSM download fallback instead of QGIS

# ── Deep Learning (PyTorch) ───────────────────────────────────────────────────
# NOTE: Install PyTorch manually based on your CUDA version.
# GPU (CUDA 12.1): pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# CPU only:        pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch
torchvision

# ── Graph Neural Networks (PyTorch Geometric) ─────────────────────────────────
# NOTE: Install after PyTorch: pip install torch-geometric
# Optional extensions (may need matching torch/cuda build):
#   pip install torch-scatter torch-sparse torch-cluster torch-spline-conv
#       -f https://data.pyg.org/whl/torch-<version>+<cuda>.html
torch-geometric

# ── Reinforcement Learning ────────────────────────────────────────────────────
stable-baselines3[extra]>=2.0.0
gymnasium>=0.29.0

# ── SUMO (Simulation of Urban MObility) ──────────────────────────────────────
# NOTE: SUMO must be installed on the system first: https://sumo.dlr.de/docs/Installing/
# Then set SUMO_HOME environment variable to the SUMO installation directory.
traci>=1.19.0
sumolib>=1.19.0
